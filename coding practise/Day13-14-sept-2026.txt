# Day13 - Advanced Automation with CloudFormation and CI/CD

#Ansible - Multi-Node Nginx Setup

- name: Multi-Node Nginx Setup
  host: all
  become: yes
  tasks:  
    - name: Install nginx
      yum:
        name: nginx
        state: present
    - name: Start nginx service
      service:
        name: nginx
        state: started
        enabled: yes

# Terraform - Create IAM Users and Policies

resource "aws_iam_user" "example_user" {
  name = "pathnex_user"
}

resource "aws_iam_policy" "example_policy" {
  name		= "pathnex-policy"
  description	= "Policy for Pathnex application"
  policy	= jsonencode({
    Version 	= "2012-10-17"
    Statement	= [
      {
	Action = "S3:ListBucket"
	Effect = "Allow"
	Resource = "arn:aws:s3:::pathnex-bucket"
      },
    ]
  })
 }

# Kubernetes - Deploy Ngnix with CongigMap and Secrets

apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
 data:
   nginx.conf: |
     server {
       listen 80;
       server_name localhost;
       location / {
         root  /usr/share/nginx/html;
	 index index.html index.htm;
       }
     }

---
apiVersion: v1
kind: Secret
metadata:
  name:
 type: Opaque
 data:
   password: cGF0aG51eHBhc3M=

---
apiVersion: app/v1
kind: Deployment
metadata: 
  name: pathnex-nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pathnex-nginx
  template:
    metadata:
      lables:
        app: pathnex-nginx
    spec:
      container:
        - name: nginx 
          image: nginx
          volumeMounts:
            - mountPath: /etc/nginx/nginx.conf
              name: nginx-config
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-secret
        - name: nginx-secret
          secret:
            secretName: nginx-secret

# Jenkinsfile - Multi-Branch Pipline with Docker Build

pipline {
   agent any
   stages {
       stage('Build') {
           steps {
               script {
 		   docker.build('Pathnex-app')
	       }
   	   }
 	}
 	stage('Deploy') {
            steps {
	        script {
                    kubectl apply -f kubenetes/deployment.yaml
                }
            }
        }
    }
}

# GitLab CI/CD - Advanced Deployment with Helm

stages:
  - build
  - push
  - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-web-app .

push:
  stage: push
  script: 
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push pathnex-web-app

deploy:
  stage: deploy
  script:
    - helm repo add pathnex https://charts.pathnex.com
    - helm install pathnex-nginx pathnex/nginx-ingress

# Docker
# COPY VS ADD

FROM ubuntu
COPY pathnex-file.txt /opt/pathnex/files/
ADD pathnex.tar.gz /opt/pathnex/extracted/
 
